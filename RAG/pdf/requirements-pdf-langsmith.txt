# Dependencias para el RAG de PDFs con LangChain + LangSmith (rag_pdf_langsmith.py).
#
# Requiere ademas:
#   - Ollama corriendo con:  ollama pull gemma3 ; ollama pull nomic-embed-text
#   - (opcional) API key de LangSmith en .env para ver las trazas.
#
# Instalar con:  pip install -r requirements-pdf-langsmith.txt

langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-ollama>=0.2
langchain-chroma>=0.2
chromadb>=0.5
pypdf>=4.0
langsmith>=0.1
python-dotenv>=1.0

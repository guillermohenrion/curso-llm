# Copia este archivo a ".env" (sin ".example") y completa tu token real.
# El archivo ".env" esta en .gitignore: nunca se sube al repo.
#
# Conseguir un token gratis: https://huggingface.co/settings/tokens (permiso "Read" alcanza)

HF_TOKEN=
